--- a/package.json
+++ b/package.json
@@
   "scripts": {
-    "dev": "vite",
-    "build": "vite build",
+    "dev": "vite",
+    "build": "vite build && node scripts/generate-sitemap.mjs",
     "preview": "vite preview"
   },
   "dependencies": {
+    "react-helmet-async": "^1.3.0"
   }
